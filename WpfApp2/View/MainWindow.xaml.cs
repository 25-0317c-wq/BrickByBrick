using System.Windows;

namespace BrickByBrick.View
{
    /// <summary>
    /// Interaction logic for MainWindow.xaml
    /// This is now the real login screen (username/password + Google + create account).
    /// UI-only for now — no authentication logic wired yet.
    /// </summary>
    public partial class MainWindow : Window
    {
        public MainWindow()
        {
            InitializeComponent();
        }
    }
}
